import { toggleForm } from '../src/client/js/app'

describe('testing the functionality for displaying the search form', () => {
    test('toggleForm() function exists', () => {
        expect(toggleForm).toBeDefined()
    })
})